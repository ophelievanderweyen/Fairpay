backup / restore 

QUOI ?

- code (html, css, js, php, img, background img, ...)  FTP, GIT
- données (db / fichiers (img, css))


Les image pq elle sont dans des fichier ? ou pq elles sont dans la db ? quelles sont les avantages ? 
Question de sécurité, performance etc 

QUAND est ce qu'on va faire notre sauvegarde ?
=> Avant (si jamais la modif ne va pas, savoir revenir en arrière) /Après une grosse modification 
=> Quand il y a le moins de trafic sur le site/app 
Si le public cible change, il va falloir changer le moment ou l'on fais des modifs 

COMMENT on fais la sauvegarde ? 
Manière : 
    - sauvegarte complète 
    - incrémentale : on enregistre que ce qui a changer depuis la dernière fois (moins gros et + rapide donc coupure moins lente) => PLUS INTERESSANTE

Format : 
    -archive (.zip, .7z, .tar.gz (double compression, le tar puis gz) cryptée ) : de la moins intéréssante a la plus intéressante
    -cryptée

QUI ? :
    - cron/script (sa serait bien que il y ait un script qui fasse la sauvegarde)

OU ? 
- ailleurs (ex :cloud/FTP) RAIDS

POURQUOI ? POUR QUI ?
- Pour faire un restore (avec doc/script) !!!!!!! Maria DB

Cout ? ca coute du CPU et de l'espace

COMBIEN ?


